= form_for(@task, html: {class: 'form-horizontal'}) do |f|
  - if @task.errors.any?
    #error_explanation
      %h2
        = pluralize(@task.errors.count, "error")
        prohibited this task from being saved:
      %ul
        - @task.errors.full_messages.each do |message|
          %li= message
  .field.form-group
    = f.label :description, class: "col-md-1"
    = f.text_field :description, class: "col-md-4"

  .field.form-group
    = f.label :due, class: "col-md-1"
    = f.date_field :due_date, class: "col-md-4"

  - if @task.id
    .field.form-group
      .col-sm-offset-1
        = f.check_box :complete
        = f.label :complete
  .actions.form-group
    = f.submit class: "btn btn-primary col-md-offset-1"


-# - render if (params[:edit])
